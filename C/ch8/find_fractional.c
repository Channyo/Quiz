#include<stdio.h>

enum {
    vertical_low = 0,
    horizontal_low,
    vertical_plus,
    horizontal_plus,
};

int main(void) {
    long x;
    int vertical = 1, horizontal = 1, state = 0;

    scanf("%ld", &x);

    switch(state) {
        case vertical_low : 
            break;

        case horizontal_low :
            break;

        case vertical_plus :
            break;

        case horizontal_plus :
            break;
    }


}